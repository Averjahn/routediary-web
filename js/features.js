/**
 * Что показывать в этой сборке.
 *
 * Флаг, а не удаление кода: незрелая функция убирается с глаз, но остаётся
 * в репозитории вместе со своими тестами. Вернуть — поменять одно значение,
 * а не восстанавливать удалённое из истории.
 */

/**
 * Светофоры: ручной замер фаз и общая копилка наблюдений.
 *
 * Выключено в MVP. Функция интересная, но сырая: чтобы вывести цикл одного
 * перекрёстка, нужны десятки наблюдений из разных дней, а пока людей мало,
 * она честно отвечает «не знаю» — и при этом занимает на экране настроек
 * больше места и внимания, чем всё остальное вместе взятое. Первое
 * впечатление о приложении она портит сильнее, чем приносит пользы.
 *
 * Когда включать обратно: когда наблюдений в копилке хватит, чтобы
 * большинство перекрёстков в городе отвечали числом, а не счётчиком сбора.
 */
export const SIGNALS_ENABLED = false;

/**
 * Диагностика: чтение кодов неисправностей из машины и их расшифровка.
 *
 * Выключено до конца внедрения. Код, разбор ответа адаптера и расшифровки
 * готовы и покрыты тестами, но ни разу не проверены на настоящей машине с
 * настоящим адаптером — а цена ошибки здесь выше обычной: человек может
 * по такому экрану решить, ехать ему или вставать.
 *
 * Отдельно ждёт решения iPhone: в Safari Web Bluetooth не существует, и
 * там чтение возможно только из нативного приложения. Показывать половине
 * пользователей раздел, который у них работает лишь наполовину, рано.
 *
 * Когда включать: после проверки на живой машине хотя бы на паре адаптеров
 * и решения по iPhone. Ручной ввод кода можно открыть и раньше — он ни от
 * какого железа не зависит.
 */
export const OBD_ENABLED = false;
